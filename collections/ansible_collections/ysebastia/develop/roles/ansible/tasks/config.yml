---
- name: "Include Python"
  ansible.builtin.include_role:
    name: "python"
- name: Ensure that Ansible is installed
  ansible.builtin.pip:
    name:
      - ansible-core==2.16.6
      - ansible-lint==24.5.0
      - yamllint==1.35.1
    virtualenv: '{{ python_venv_directory }}'
