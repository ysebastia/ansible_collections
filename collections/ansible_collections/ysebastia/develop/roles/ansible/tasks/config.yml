---
- name: "Include Python"
  ansible.builtin.include_role:
    name: "python"
- name: Ensure that Ansible is installed
  ansible.builtin.pip:
    name:
      - ansible-core==2.16.3
      - ansible-lint==6.22.2
      - yamllint==1.34.0
    virtualenv: '{{ python_venv_directory }}'
