---
- name: Ensure packages are installed
  become: true
  ansible.builtin.apt:
    name:
      - ca-certificates-java
- name: Ensure packages are installed
  become: true
  ansible.builtin.apt:
    name:
      - ant
      - git
      - make
      - openjdk-21-jre-headless
      - pipx
      - python3.11-venv
      - wget
