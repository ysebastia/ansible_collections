---
- name: Converge
  hosts: all
  tasks:
    - name: "Include bootstrap"
      ansible.builtin.include_role:
        name: "ysebastia.base.bootstrap"
      vars:
        enable_repositories_epel: true
        epel_https_proxy: "{{ ansible_facts.env.HTTP_PROXY }}"
