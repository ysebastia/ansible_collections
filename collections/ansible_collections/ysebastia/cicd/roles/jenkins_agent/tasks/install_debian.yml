---
- name: Ensure packages are installed
  become: true
  ansible.builtin.apt:
    name:
      - ca-certificates-java
- name: Ensure packages are installed
  become: true
  ansible.builtin.apt:
    name:
      - ant
      - git
      - make
      - openjdk-17-jre-headless
      - wget
