---
- name: Facts
  ansible.builtin.include_tasks: "facts.yml"

- name: Configure APACHE if enable_service_apache is defined
  ansible.builtin.include_tasks: "{{ ansible_action }}.yml"
  when: enable_service_apache
