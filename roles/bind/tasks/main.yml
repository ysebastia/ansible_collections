---
- name: Gather facts
  ansible.builtin.gather_facts:

- name: Configure BIND if enable_service_bind is defined
  ansible.builtin.include_tasks: "{{ ansible_action }}.yml"
  when: enable_service_bind
