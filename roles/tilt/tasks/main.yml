---
- name: Install Tilt
  when: enable_tilt
  block:
    - name: Download tilt
      become: true
      ansible.builtin.unarchive:
        src: "{{ tilt_repo }}"
        dest: /usr/local/bin/
        owner: root
        group: root
        mode: "0755"
        remote_src: true
