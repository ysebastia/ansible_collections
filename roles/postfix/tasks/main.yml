---
- name: Gather facts
  ansible.builtin.gather_facts:

- name: Configure POSTFIX if enable_service_postfix is defined
  ansible.builtin.include_tasks: "{{ ansible_action }}.yml"
  when: enable_service_postfix
