---
- name: Ensure that Unzip package is present
  ansible.builtin.package:
    name:
      - unzip
    state: present
- name: Download k9s
  become: true
  ansible.builtin.unarchive:
    src: "{{ k9s_repo }}"
    dest: /usr/local/bin/
    owner: root
    group: root
    mode: "0755"
    remote_src: true
