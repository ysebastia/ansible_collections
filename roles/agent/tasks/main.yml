---
- name: Gather facts
  ansible.builtin.gather_facts:

- name: Configure BackupPC agent if enable_backuppc_plug is defined
  ansible.builtin.include_tasks: "{{ ansible_action }}.yml"
  when: enable_backuppc_agent
