---
- name: "Include Python"
  ansible.builtin.include_role:
    name: "python"
- name: Ensure that Ansible is installed
  ansible.builtin.pip:
    name:
      - ansible-core==2.16.1
      - ansible-lint==6.22.1
      - yamllint==1.33.0
    virtualenv: '{{ python_venv_directory }}'
