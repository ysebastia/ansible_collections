---
- name: "Include Python"
  ansible.builtin.include_role:
    name: "python"
- name: Ensure that Ansible is installed
  ansible.builtin.pip:
    name:
      - ansible-core==2.15.4
      - ansible-lint==6.20.0
      - yamllint==1.32.0
    virtualenv: '{{ python_venv_directory }}'
